# Dataverse Environment Configuration
# Copy this file to .env and fill in your values.
# These values are used for local development and deployment only.

# Target Dataverse environment URL
# Example: https://your-org.crm.dynamics.com
DATAVERSE_URL=

# Authentication
# Run: pac auth create --url $DATAVERSE_URL
# This creates a local auth profile. No secrets are stored in this file.
#
# To list auth profiles:  pac auth list
# To switch profiles:     pac auth select --index <n>
# To delete a profile:    pac auth delete --index <n>
